version: '3.8'
services:
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
  backend:
    build: ./backend
    ports: ["8000:8000"]
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: phare_blanc
      POSTGRES_PASSWORD: password
